version: '2'
services:

  postgres:
    container_name: as_postgres
    build: ./postgres
    volumes:
      - data:/var/lib/postgresql/data
    ports:
       - "5400:5432"

  redis:
    container_name: as_redis
    build: ./redis
    ports:
       - "6379:6379"
